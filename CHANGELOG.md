# 更新日志

## 2.0.0 - 2026-08-16

- 新增 96×96 自动分片模式：先对整张图片完成统一采样、颜色调整、抖动和色板量化，再拆成 16 张 24×24 子画。
- 按游戏画像册的反向槽位规则，从右下到左上绘制和保存，使 16 张草稿最终组成方向正确的 4×4 画面。
- 自动完成“进入编辑页、绘制、保存草稿、新增下一张”的完整流程；第 16 张保存后停留在画像册，不会点击“完成并发布”。
- 增加分片级断点续画和布局版本校验；相同作品可从下一张继续，旧排列规则的进度会自动失效。
- 页面进入、单片绘制和保存失败时最多自动尝试 3 次；保存重试具备幂等判断，已返回画像册时不会重复点击。
- 96×96 模式默认使用居中裁切铺满预览，避免非正方形图片产生上下或左右白边。
- 增加非纯白浅色防漏检查，暖白、淡粉白、象牙白等颜色出现纯白漏点时会自动补画，即使关闭常规安全校验也会保留该检查。
- 新增 4×4 分片边界预览、9216 格总进度、续画提示和清除续画进度入口；24×24 单画模式保持兼容。

## 1.2.3 - 2026-08-14

- 将“实验性画布校验”更名为“实验性快速校验”，更清楚地表达其跳过已匹配格子、减少点击次数的用途。
- 明确快速校验与绘制安全校验为独立开关；关闭安全校验时，快速校验仍可单独启用。
- 完善界面提示、运行日志、README 和使用说明，保留原有设置字段以兼容已有配置。

## 1.2.2 - 2026-08-11

- 新增桌面区域截图导入：点击“截图”后可在当前显示器上拖动框选，并直接转换为 24×24 像素画。
- 截图遮罩适配多显示器和混合 DPI，支持显示选区尺寸、回车确认、Esc 取消以及贴近屏幕边缘的选区。
- 截图和 PNG 编码在后台执行，关闭或取消截图后主窗口会自动恢复，避免界面长时间卡顿或隐藏。
- 修复 GDI 截图位图生命周期、预览编码并发释放及屏幕边缘裁切越界问题。
- 截图临时文件会在下次截图或程序正常退出时清理。

## 1.2.1 - 2026-08-11

- 修复电脑版在高于 1080P 或非等比界面下第一排颜料点击偏移；MaaFramework 截图统一到 1080 短边坐标基准，并按实际色块边缘动态定位每一行。
- 修复部分 ADB 截图带前导信息、截断或直接截图异常时出现 `codec null`；增加图片格式完整性检查和设备端临时截图降级路径。
- 改进选中色块验证，结合点击前后截图差分与更宽的矩形环带识别，兼容右下角色块、纯白颜料和近白色青色发光边框。
- 改进落色结果验证，按格内目标颜色占比判断，降低斜向 UID 水印和中心遮挡导致的误报。
- 漏格重试改为真实点击，避免零长度滑动在部分输入后端上失效。
- 手动校准允许颜料框包含“颜料”标题栏，但仍应排除撤销、保存等上方工具区域。

## 1.2.0 - 2026-08-09

- 新增亮度、对比度和饱和度调整，范围均为 `-100～+100`，支持一键归零。
- 图像调整在 24×24 采样后、抖动和固定色板量化前生效，预览与实际绘制结果一致。
- 新增实验性滑动绘制；同一行连续至少 3 个同色格会合并为直线滑动，其他格子继续批量点击。
- 滑动绘制支持模拟器 ADB 与原生电脑版 MaaFramework 后端，并沿用暂停、取消、视觉校验和漏格补画。

## 1.1.1 - 2026-08-09

- 新增 SVG 图片导入、拖放、裁切和 24×24 转换支持。
- SVG 栅格化最长边限制为 2048 像素，并保留透明通道。
- 禁止 SVG 脚本、事件处理器和外部资源，单个文件限制为 8 MB。

## 1.1.0 - 2026-08-09

- 新增原生电脑版兼容模式，复用 MaaFramework 获取窗口画面并发送输入；选择电脑版时会按需申请管理员权限。
- 将模拟器和电脑版连接方式改为并排按钮，并支持自动发现电脑版游戏进程或手动填写 PID。
- 内置 Android Platform Tools ADB 35.0.2；配置路径、模拟器 ADB、Android SDK 和 PATH 均不可用时自动降级使用。
- 新增“绘制安全校验”开关，可在视觉校验误报时直接绘制；设备、校准和颜料定位检查仍然保留。
- 新增 `F8` 暂停/继续快捷键，并完善按钮状态和界面说明。
- 优化手动校准指引，明确画布框贴紧边界、颜料框只覆盖下半区色板。
- 标题栏新增 B站主页和 GitHub 项目入口。
- 修复管理员权限下无法打开图片、启动卡死及 PC 窗口截图包含无关窗口等问题。
- 发布包统一为 .NET 10 自包含 win-x64 ZIP，包含运行所需组件和中文文档。

## 1.0.0 - 2026-08-09

首个正式版本。

- 支持 PNG、JPEG、BMP、WebP 图片导入、拖放与交互式裁切。
- 支持完整适配、居中裁切、拉伸三种构图方式。
- 提供拼豆均色、拼豆主色、感知平滑三种转换算法。
- 提供无抖动、Atkinson、Floyd-Steinberg、Bayer 4×4 四种抖动模式。
- 使用 CIEDE2000 将图片映射到内置 40 色固定颜料色板。
- 支持 ADB 自动发现、TCP 连接、自动或手动画布校准。
- 支持按颜色分组绘制、暂停、继续、取消及漏格重试。
- 提供 Windows 11 风格的 WinUI 3 界面与 .NET 10 单文件版本。
